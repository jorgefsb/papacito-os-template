import React from 'react'
import ReactDOM from 'react-dom/client'
import ExecutiveView from './dashboard/ExecutiveView.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <ExecutiveView />
    </React.StrictMode>,
)
